vars:

tid i

invariant [hashmap_levels] :

//  @myLock_set(i). -> add::myLock(i) < locksLength

#myBucket_set:
  @myBucket_set(i). -> add::myBucket(i) < capacity
  


//  @locked_bucket(i). ->
//    locks[myLock] = i
