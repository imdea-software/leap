
vars:

tid j

invariant [queueReach] :

//   @enqueue_last_was_tail(j). -> tail in addr2set(heap, enqueue::last(j))
 @dequeue_last_was_tail(j). -> tail in addr2set(heap, dequeue::last(j))
/\ @dequeue_last_was_tail(j). -> dequeue::last(j) in addr2set(heap, dequeue::first(j))

/\ @next_follows_first(j). -> dequeue::nextptr(j) in addr2set(heap, dequeue::first(j))
/\ @dequeue_first_was_head(j). -> head in addr2set(heap, dequeue::first(j))
