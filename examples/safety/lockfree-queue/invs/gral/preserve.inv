vars:

tid j

invariant [queuePreserve] :

   null in region
/\ tail in region
/\ tail != null
/\ head != null
/\ region = addr2set(heap, head)
/\ @next_follows_first(j). -> tail in addr2set(heap, head)

